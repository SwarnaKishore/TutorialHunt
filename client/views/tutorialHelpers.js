
Template.Python.helpers({
 Python: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Python"},{sort:  {score : -1}});
}
});

Template.Wordpress.helpers({
 Wordpress: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Wordpress"},{sort:  {score : -1}});
}
});


Template.AndroidDevelopment.helpers({
 AndroidDevelopment: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"AndroidDevelopment"},{sort:  {score : -1}});
}
});


Template.AngularJs.helpers({
 AngularJs: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"AngularJs"},{sort:  {score : -1}});
}
});


Template.Backbone.helpers({
 Backbone: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Backbone"},{sort:  {score : -1}});
}
});


Template.Bootstrap.helpers({
 Bootstrap: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Bootstrap"},{sort:  {score : -1}});
}
});

Template.CAndCPlusPlus.helpers({
 CAndCPlusPlus: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"CAndCPlusPlus"},{sort:  {score : -1}});
}
});

Template.CSharp.helpers({
 CSharp: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"CSharp"},{sort:  {score : -1}});
}
});

Template.Django.helpers({
 Django: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Django"},{sort:  {score : -1}});
}
});


Template.Express.helpers({
 Express: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Express"},{sort:  {score : -1}});
}
});


Template.Git.helpers({
 Git: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Git"},{sort:  {score : -1}});
}
});


Template.Go.helpers({
 Go: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Go"},{sort:  {score : -1}});
}
});



Template.Hadoop.helpers({
 Hadoop: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Hadoop"},{sort:  {score : -1}});
}
});


Template.HTMLandCSS.helpers({
 HTMLandCSS: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"HTMLandCSS"},{sort:  {score : -1}});
}
});


Template.Java.helpers({
 Java: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Java"},{sort:  {score : -1}});
}
});


Template.Javascript.helpers({
 Javascript: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Javascript"},{sort:  {score : -1}});
}
});


Template.Jquery.helpers({
 Jquery: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Jquery"},{sort:  {score : -1}});
}
});


Template.Meteor.helpers({
 Meteor: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Meteor"},{sort:  {score : -1}});
}
});


Template.Mongodb.helpers({
 Mongodb: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Mongodb"},{sort:  {score : -1}});
}
});


Template.Mysql.helpers({
 Mysql: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Mysql"},{sort:  {score : -1}});
}
});


Template.NodeJs.helpers({
 NodeJs: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"NodeJs"},{sort:  {score : -1}});
}
});


Template.Perl.helpers({
 Perl: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Perl"},{sort:  {score : -1}});
}
});



Template.PHP.helpers({
 PHP: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"PHP"},{sort:  {score : -1}});
}
});


Template.Photoshop.helpers({
 Photoshop: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Photoshop"},{sort:  {score : -1}});
}
});





Template.React.helpers({
 React: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"React"},{sort:  {score : -1}});
}
});


Template.RubyOnRails.helpers({
 RubyOnRails: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"RubyOnRails"},{sort:  {score : -1}});
}
});


Template.Ruby.helpers({
 Ruby: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"Ruby"},{sort:  {score : -1}});
}
});


Template.SQL.helpers({
 SQL: function()
 {
  var currentUserId = Meteor.userId();
  return tutorialList.find({category:"SQL"},{sort:  {score : -1}});
}
});





